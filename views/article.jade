extends layout

block content

	- if (!results._id)
		h2.ui.ten.wide.column 
		| Article "<strong>#{id}</strong>" does not exist.
	- else

		div.ui.segment
			h1.ui.dividing.header: != results.title
			div.ui.items
				div.item
					div.content
						!= results.html

		include annotation
